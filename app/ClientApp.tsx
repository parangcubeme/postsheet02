"use client";

import { useMemo, useState } from "react";

type Row = Record<string, unknown>;
type MarketTab = "smartstore" | "esm";
type StepId = "category" | "notice" | "origin" | "shipping" | "price" | "review";
type Product = {
  id: string; raw: Row; productName: string; sellerCode: string; basePrice: number; finalPrice: number; stock: number;
  mainImage: string; additionalImage: string; detailHtml: string; shippingFee: number; vatType: string;
  categoryGroup: string; categoryCode: string; auctionExposureCode: string; gmarketExposureCode: string; productGroupCode: string;
  noticeTemplateCode: string; originProductType: string; originRegionType: string; originRegionCode: string; multipleOrigins: string;
  shippingGroup: string; departureCode: string; shippingPolicyNumber: string; returnAddressCode: string;
  auctionShippingPolicy: string; gmarketShippingPolicy: string; courierCode: string; returnShippingFee: number;
};
type DownloadPage = { key: string; category: string; page: number; items: Product[] };

const aliases = {
  productName: ["상품명", "제품명", "상품이름", "품명", "상품 명"],
  sellerCode: ["판매자 상품코드", "판매자상품코드", "상품코드", "자체상품코드", "관리코드", "품목코드", "판매자코드"],
  basePrice: ["판매가", "판매가격", "상품가격", "상품 판매가", "공급가", "공급가격", "판매단가", "단가", "기준가격", "원가", "매입가", "소비자가", "정상가", "가격"],
  stock: ["재고수량", "재고", "수량", "판매가능수량", "A 재고", "G 재고"],
  mainImage: ["대표이미지", "대표이미지URL", "이미지1", "메인이미지", "이미지URL", "기본이미지"],
  detailHtml: ["상세설명", "상품설명", "상세HTML", "상품상세", "상세페이지", "상세이미지", "상품상세설명"],
  shippingFee: ["기본배송비", "배송비", "반품/교환 배송비"],
  vatType: ["부가세", "과세구분", "부가세유형", "부가세여부"],
  originCode: ["원산지코드", "원산지 지역코드"],
  originDirect: ["원산지 직접입력", "원산지직접입력", "원산지", "원산지명"],
};
const naverHeaders = ["판매자 상품코드", "카테고리코드", "상품명", "상품상태", "판매가", "부가세", "재고수량", "대표이미지", "추가이미지", "상세설명", "원산지코드", "복수원산지여부", "원산지 직접입력", "미성년자 구매", "배송방법", "택배사코드", "배송비유형", "기본배송비", "반품배송비", "교환배송비", "별도설치비", "A/S 전화번호", "A/S 안내", "구매평 노출여부", "알림받기 동의 고객 전용 여부"];
const esmHeaders = ["노출\n사이트", "A ID", "G ID", "상품명", "A프로모션 문구", "G프로모션 문구", "G 영문", "G 중문", "카테고리 템플릿 코드", "카테고리 코드", "A 노출코드", "G 노출코드", "판매기간", "A 판매가", "G 판매가", "A 할인유형", "A 할인가", "G 할인유형", "G 할인가", "A 재고", "G 재고", "옵션\n타입", "옵션명", "옵션\n입력값", "기본이미지", "추가이미지", "상품상세설명", "배송정보 \n템플릿 코드", "배송방법", "출하지 코드", "배송정책번호", "반품/교환\n주소 코드", "A 발송정책", "G 발송정책", "택배사\n코드", "반품/교환\n배송비", "상품군\n코드", "상품고시정보\n템플릿코드", "인증타입", "인증품목선택", "인증코드", "인증타입", "인증품목선택", "인증코드", "병행수입여부", "인증타입", "인증품목선택", "인증코드", "병행수입여부", "인증타입", "승인/신고번호", "원산지\n상품타입", "원산지\n지역타입", "원산지\n지역코드", "복수\n원산지여부", "사은품/덤 \n템플릿 코드", "사은품", "덤", "소비기한", "제조일자", "청소년구매\n불가여부", "부가세여부", "선물하기상품"];
const steps: { id: StepId; label: string }[] = [
  { id: "category", label: "1. 카테고리" }, { id: "notice", label: "2. 고시정보" }, { id: "origin", label: "3. 원산지" },
  { id: "shipping", label: "4. 배송정책" }, { id: "price", label: "5. 가격" }, { id: "review", label: "6. 다운로드" },
];
const fieldStyle: React.CSSProperties = { display: "grid", gap: 6 };
const gridStyle: React.CSSProperties = { display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(210px,1fr))", gap: 12 };
const buttonStyle: React.CSSProperties = { padding: "10px 14px", borderRadius: 8, cursor: "pointer" };

function cleanKey(v: unknown) { return String(v ?? "").replace(/[\s\n\r_\-()\[\]\/]/g, "").toLowerCase(); }
function normalize(v: unknown) { return String(v ?? "").trim(); }
function digits(v: unknown) { return normalize(v).replace(/\D/g, ""); }
function numberValue(v: unknown) { const n = Number(String(v ?? "").replace(/,/g, "").replace(/[^0-9.-]/g, "")); return Number.isFinite(n) ? n : 0; }
function pickExact(row: Row, name: string) { const target = cleanKey(name); const found = Object.entries(row).find(([key]) => cleanKey(key) === target); return found ? found[1] : ""; }
function pick(row: Row, names: string[]) { for (const name of names) { const value = pickExact(row, name); if (normalize(value)) return value; } return ""; }
function detectGroup(name: string) {
  const text = name.toLowerCase();
  if (/티셔츠|셔츠|바지|원피스|의류/.test(text)) return { group: "의류 예상", productGroupCode: "1" };
  if (/신발|구두|운동화|샌들/.test(text)) return { group: "신발 예상", productGroupCode: "2" };
  if (/가방|백팩|파우치/.test(text)) return { group: "가방 예상", productGroupCode: "3" };
  if (/만두|과자|라면|커피|음료|빵|떡/.test(text)) return { group: "가공식품 예상", productGroupCode: "21" };
  return { group: "미분류", productGroupCode: "35" };
}
function makeProducts(rows: Row[]) {
  return rows.map((raw, index): Product => {
    const productName = normalize(pick(raw, aliases.productName)); const basePrice = numberValue(pick(raw, aliases.basePrice));
    const existingCategory = digits(pickExact(raw, "카테고리코드")); const detected = detectGroup(productName);
    const shippingGroup = ["출하지 코드", "배송정책번호", "A 발송정책", "G 발송정책"].map(k => normalize(pickExact(raw, k))).filter(Boolean).join(" / ") || "배송정보 미입력";
    return { id: `P${index + 1}`, raw, productName, sellerCode: normalize(pick(raw, aliases.sellerCode)) || `P${index + 1}`, basePrice, finalPrice: basePrice,
      stock: numberValue(pick(raw, aliases.stock)) || 99999, mainImage: normalize(pick(raw, aliases.mainImage)), additionalImage: normalize(pickExact(raw, "추가이미지")) || normalize(pickExact(raw, "이미지2")),
      detailHtml: normalize(pick(raw, aliases.detailHtml)), shippingFee: numberValue(pick(raw, aliases.shippingFee)), vatType: normalize(pick(raw, aliases.vatType)),
      categoryGroup: existingCategory ? `기존 카테고리 ${existingCategory}` : detected.group, categoryCode: existingCategory,
      auctionExposureCode: digits(pickExact(raw, "A 노출코드")), gmarketExposureCode: digits(pickExact(raw, "G 노출코드")), productGroupCode: digits(pickExact(raw, "상품군 코드")) || detected.productGroupCode,
      noticeTemplateCode: digits(pickExact(raw, "상품고시정보 템플릿코드")) || "239479", originProductType: normalize(pickExact(raw, "원산지 상품타입")) || "해당없음",
      originRegionType: normalize(pickExact(raw, "원산지 지역타입")) || "알수없음", originRegionCode: digits(pick(raw, aliases.originCode)), multipleOrigins: normalize(pickExact(raw, "복수 원산지여부")) || "단일원산지",
      shippingGroup, departureCode: digits(pickExact(raw, "출하지 코드")), shippingPolicyNumber: digits(pickExact(raw, "배송정책번호")), returnAddressCode: digits(pickExact(raw, "반품/교환 주소 코드")),
      auctionShippingPolicy: digits(pickExact(raw, "A 발송정책")), gmarketShippingPolicy: digits(pickExact(raw, "G 발송정책")), courierCode: digits(pickExact(raw, "택배사 코드")) || "10013",
      returnShippingFee: numberValue(pickExact(raw, "반품/교환 배송비")) || 2500 };
  }).filter(item => item.productName && item.basePrice > 0);
}
function groupProducts(products: Product[], step: StepId) {
  const map = new Map<string, Product[]>();
  for (const product of products) {
    const key = step === "category" ? product.categoryGroup : step === "notice" ? `${product.categoryCode || "카테고리 미입력"} / ${product.noticeTemplateCode}` : step === "origin" ? product.originRegionCode || "원산지 미입력" : step === "shipping" ? product.shippingGroup : "전체";
    const current = map.get(key); if (current) current.push(product); else map.set(key, [product]);
  }
  return [...map.entries()].map(([name, items]) => ({ name, items }));
}
function makeEsmRows(products: Product[], auctionId: string, gmarketId: string) {
  const top: unknown[][] = [["▶가이드 바로가기", null, " ※ 문서버전 : NEW 2.0", null, "필독▶ 일반배송 전용 파일입니다. 상품정보는 8행부터 입력됩니다.", ...Array(59).fill(null)], [null, "상품기본정보", ...Array(26).fill(null), "배송정보", ...Array(8).fill(null), "상품고시정보", ...Array(25).fill(null)], [null, ...esmHeaders], [null, ...esmHeaders.map(() => "")], [null, ...esmHeaders.map(() => "")], [null, ...esmHeaders.map(() => "")], [null, ...esmHeaders.map(() => "")]];
  return [...top, ...products.map(product => { const row = Array(64).fill(""); row[1] = "옥션/G마켓"; row[2] = auctionId; row[3] = gmarketId; row[4] = product.productName; row[10] = product.categoryCode; row[11] = product.auctionExposureCode; row[12] = product.gmarketExposureCode; row[13] = "무제한"; row[14] = product.finalPrice; row[15] = product.finalPrice; row[20] = product.stock; row[21] = product.stock; row[22] = "미사용"; row[25] = product.mainImage; row[26] = product.additionalImage; row[27] = product.detailHtml; row[29] = "일반택배"; row[30] = product.departureCode; row[31] = product.shippingPolicyNumber; row[32] = product.returnAddressCode; row[33] = product.auctionShippingPolicy; row[34] = product.gmarketShippingPolicy; row[35] = product.courierCode; row[36] = product.returnShippingFee; row[37] = product.productGroupCode; row[38] = product.noticeTemplateCode; row[39] = "인증대상아님"; row[42] = "인증대상아님"; row[45] = "해당사항없음"; row[46] = "인증대상아님"; row[49] = "해당사항없음"; row[50] = "인증대상아님"; row[52] = product.originProductType; row[53] = product.originRegionType; row[54] = product.originRegionCode; row[55] = product.multipleOrigins; row[61] = "구매가능"; row[62] = product.vatType.includes("면세") ? "면세상품" : "과세상품"; row[63] = "가능"; return row; })];
}

export default function ClientApp() {
  const [active, setActive] = useState<MarketTab>("smartstore"); const [rows, setRows] = useState<Row[]>([]); const [products, setProducts] = useState<Product[]>([]);
  const [status, setStatus] = useState("상품 엑셀을 업로드해 주세요."); const [busy, setBusy] = useState(false);
  const [feeRate, setFeeRate] = useState(6); const [smartMargin, setSmartMargin] = useState(30); const [extraCost, setExtraCost] = useState(0); const [smartRound, setSmartRound] = useState(100);
  const [smartCategory, setSmartCategory] = useState("50001770"); const [smartCourier, setSmartCourier] = useState("CJGLS"); const [asPhone, setAsPhone] = useState("01027483227");
  const [step, setStep] = useState<StepId>("category"); const [selectedGroup, setSelectedGroup] = useState(""); const [groupPage, setGroupPage] = useState(0);
  const [auctionId, setAuctionId] = useState(""); const [gmarketId, setGmarketId] = useState(""); const [marginRate, setMarginRate] = useState(0); const [roundUnit, setRoundUnit] = useState(100);
  const groups = useMemo(() => groupProducts(products, step), [products, step]); const visibleGroups = useMemo(() => groups.slice(groupPage * 40, groupPage * 40 + 40), [groups, groupPage]); const currentGroup = groups.find(group => group.name === selectedGroup) ?? groups[0];
  const downloadPages = useMemo<DownloadPage[]>(() => { const byCategory = new Map<string, Product[]>(); for (const product of products) { const key = product.categoryCode || "미분류"; const current = byCategory.get(key); if (current) current.push(product); else byCategory.set(key, [product]); } const pages: DownloadPage[] = []; for (const [category, items] of byCategory) for (let i = 0; i < items.length; i += 500) pages.push({ key: `${category}-${i}`, category, page: i / 500 + 1, items: items.slice(i, i + 500) }); return pages; }, [products]);

  async function readFile(file?: File) { if (!file) return; setBusy(true); setStatus("엑셀 기능을 불러오는 중입니다."); try { const XLSX = await import("xlsx"); const workbook = XLSX.read(await file.arrayBuffer(), { type: "array" }); const sheet = workbook.Sheets[workbook.SheetNames[0]]; const matrix = XLSX.utils.sheet_to_json<unknown[]>(sheet, { header: 1, defval: "", raw: false }); const candidates = [...aliases.productName, ...aliases.basePrice].map(cleanKey); let headerIndex = 0; let best = -1; matrix.slice(0, 30).forEach((row, index) => { const score = row.map(cleanKey).filter(Boolean).filter(key => candidates.some(c => key === c || key.includes(c) || c.includes(key))).length; if (score > best) { best = score; headerIndex = index; } }); const headers = (matrix[headerIndex] ?? []).map((value, index) => normalize(value) || `열${index + 1}`); const parsedRows = matrix.slice(headerIndex + 1).map(values => Object.fromEntries(headers.map((header, index) => [header, values[index] ?? ""]))).filter(row => Object.values(row).some(value => normalize(value))); const parsed = makeProducts(parsedRows); setRows(parsedRows); setProducts(parsed); setStep("category"); setSelectedGroup(""); setGroupPage(0); setStatus(`${parsed.length}개 상품을 읽었습니다.`); } catch { setStatus("파일을 읽지 못했습니다."); } finally { setBusy(false); } }
  function patchGroup(patch: Partial<Product>) { if (!currentGroup) return; const ids = new Set(currentGroup.items.map(item => item.id)); setProducts(current => current.map(product => ids.has(product.id) ? { ...product, ...patch } : product)); setStatus(`${currentGroup.items.length}개 상품에 적용했습니다.`); }
  async function downloadSmart() { if (!rows.length) return; setBusy(true); try { const XLSX = await import("xlsx"); const body = makeProducts(rows).map(product => [product.sellerCode, smartCategory, product.productName, "신상품", Math.ceil(((product.basePrice + extraCost) * (1 + (feeRate + smartMargin) / 100)) / Math.max(1, smartRound)) * Math.max(1, smartRound), product.vatType || "과세상품", product.stock, product.mainImage, product.additionalImage, product.detailHtml, "03", "N", normalize(pick(product.raw, aliases.originDirect)), "Y", "택배, 소포, 등기", smartCourier, product.shippingFee > 0 ? "유료" : "무료", product.shippingFee, product.shippingFee || 3000, (product.shippingFee || 3000) * 2, "N", asPhone, "판매자에 문의하시거나, A/S연락처로 문의 주시기 바랍니다.", "Y", "N"]); const sheet = XLSX.utils.aoa_to_sheet([naverHeaders, ...body]); const workbook = XLSX.utils.book_new(); XLSX.utils.book_append_sheet(workbook, sheet, "일괄등록"); XLSX.writeFile(workbook, "postsheet02_스마트스토어.xlsx"); setStatus("스마트스토어 파일 다운로드를 시작했습니다."); } finally { setBusy(false); } }
  async function downloadPage(page: DownloadPage) { setBusy(true); try { const XLSX = await import("xlsx"); const sheet = XLSX.utils.aoa_to_sheet(makeEsmRows(page.items, auctionId.trim(), gmarketId.trim())); sheet["!cols"] = Array(64).fill({ wch: 16 }); const workbook = XLSX.utils.book_new(); XLSX.utils.book_append_sheet(workbook, sheet, "NEW 일반상품"); XLSX.writeFile(workbook, `ESM_카테고리_${page.category}_${page.page}페이지_${page.items.length}개.xlsx`); setStatus("다운로드를 시작했습니다."); } finally { setBusy(false); } }
  function renderEditor() { if (!currentGroup) return <p>수정할 상품이 없습니다.</p>; const first = currentGroup.items[0]; if (step === "category") return <Editor title={currentGroup.name} count={currentGroup.items.length} sample={currentGroup.items.slice(0, 8)}><div style={gridStyle}><Input label="카테고리 코드" value={first.categoryCode} apply={value => patchGroup({ categoryCode: digits(value), categoryGroup: `카테고리 ${digits(value) || "미입력"}` })} /><Input label="A 노출코드" value={first.auctionExposureCode} apply={value => patchGroup({ auctionExposureCode: digits(value) })} /><Input label="G 노출코드" value={first.gmarketExposureCode} apply={value => patchGroup({ gmarketExposureCode: digits(value) })} /><Input label="상품군 코드" value={first.productGroupCode} apply={value => patchGroup({ productGroupCode: digits(value) })} /></div></Editor>; if (step === "notice") return <Editor title={currentGroup.name} count={currentGroup.items.length} sample={currentGroup.items.slice(0, 8)}><Input label="상품고시정보 템플릿코드" value={first.noticeTemplateCode || "239479"} apply={value => patchGroup({ noticeTemplateCode: digits(value) || "239479" })} /></Editor>; if (step === "origin") return <Editor title={currentGroup.name} count={currentGroup.items.length} sample={currentGroup.items.slice(0, 8)}><div style={gridStyle}><Input label="원산지 상품타입" value={first.originProductType} apply={value => patchGroup({ originProductType: value })} /><Input label="원산지 지역타입" value={first.originRegionType} apply={value => patchGroup({ originRegionType: value })} /><Input label="원산지 지역코드" value={first.originRegionCode} apply={value => patchGroup({ originRegionCode: digits(value) })} /></div></Editor>; if (step === "shipping") return <Editor title={currentGroup.name} count={currentGroup.items.length} sample={currentGroup.items.slice(0, 8)}><div style={gridStyle}><Input label="출하지 코드" value={first.departureCode} apply={value => patchGroup({ departureCode: digits(value) })} /><Input label="배송정책번호" value={first.shippingPolicyNumber} apply={value => patchGroup({ shippingPolicyNumber: digits(value) })} /><Input label="반품/교환 주소 코드" value={first.returnAddressCode} apply={value => patchGroup({ returnAddressCode: digits(value) })} /><Input label="A 발송정책" value={first.auctionShippingPolicy} apply={value => patchGroup({ auctionShippingPolicy: digits(value) })} /><Input label="G 발송정책" value={first.gmarketShippingPolicy} apply={value => patchGroup({ gmarketShippingPolicy: digits(value) })} /></div></Editor>; return null; }

  return <main className="container"><section className="hero"><span className="badge">postsheet02 · 가벼운 모드</span><h1>상품 엑셀 변환</h1><p>스마트스토어와 ESM을 지원하며 ZIP은 사용하지 않습니다.</p></section><section className="panel"><div className="field full"><label>상품 일괄목록 엑셀 업로드</label><input disabled={busy} type="file" accept=".xlsx,.xls,.csv" onChange={event => readFile(event.target.files?.[0])} /></div><div className="full" style={{ display: "flex", gap: 8, flexWrap: "wrap" }}><button style={buttonStyle} onClick={() => setActive("smartstore")}>스마트스토어</button><button style={buttonStyle} onClick={() => setActive("esm")}>ESM · 옥션/G마켓</button></div>{active === "smartstore" ? <><div className="full" style={gridStyle}><label style={fieldStyle}>네이버 수수료율 (%)<input type="number" value={feeRate} onChange={e => setFeeRate(Number(e.target.value))} /></label><label style={fieldStyle}>추가 마진율 (%)<input type="number" value={smartMargin} onChange={e => setSmartMargin(Number(e.target.value))} /></label><label style={fieldStyle}>상품당 추가비용<input type="number" value={extraCost} onChange={e => setExtraCost(Number(e.target.value))} /></label><label style={fieldStyle}>판매가 올림 단위<input type="number" value={smartRound} onChange={e => setSmartRound(Number(e.target.value))} /></label><label style={fieldStyle}>카테고리코드<input value={smartCategory} onChange={e => setSmartCategory(e.target.value)} /></label><label style={fieldStyle}>택배사코드<input value={smartCourier} onChange={e => setSmartCourier(e.target.value)} /></label><label style={fieldStyle}>A/S 전화번호<input value={asPhone} onChange={e => setAsPhone(e.target.value)} /></label></div><div className="actions full"><button disabled={busy || rows.length === 0} style={buttonStyle} onClick={downloadSmart}>스마트스토어 다운로드</button></div></> : <><div className="full" style={{ display: "flex", gap: 8, overflowX: "auto" }}>{steps.map(item => <button key={item.id} style={{ ...buttonStyle, whiteSpace: "nowrap", fontWeight: step === item.id ? 800 : 400 }} onClick={() => { setStep(item.id); setSelectedGroup(""); setGroupPage(0); }}>{item.label}</button>)}</div><div className="full" style={gridStyle}><label style={fieldStyle}>옥션 판매자 ID<input value={auctionId} onChange={e => setAuctionId(e.target.value)} /></label><label style={fieldStyle}>G마켓 판매자 ID<input value={gmarketId} onChange={e => setGmarketId(e.target.value)} /></label></div>{products.length > 0 && ["category", "notice", "origin", "shipping"].includes(step) && <div className="full" style={{ display: "grid", gridTemplateColumns: "minmax(220px,300px) 1fr", gap: 16 }}><aside style={{ display: "grid", gap: 8, alignContent: "start", maxHeight: "65vh", overflow: "auto", contain: "layout paint" }}><strong>{groups.length}개 묶음 · {products.length}개 상품</strong>{visibleGroups.map(group => <button key={group.name} style={{ ...buttonStyle, textAlign: "left" }} onClick={() => setSelectedGroup(group.name)}>{group.name}<br /><small>{group.items.length}개</small></button>)}{groups.length > 40 && <div style={{ display: "flex", gap: 6 }}><button disabled={groupPage === 0} onClick={() => setGroupPage(p => Math.max(0, p - 1))}>이전</button><button disabled={(groupPage + 1) * 40 >= groups.length} onClick={() => setGroupPage(p => p + 1)}>다음</button></div>}</aside><div>{renderEditor()}</div></div>}{step === "price" && <div className="full"><h2>가격 적용</h2><div style={gridStyle}><label style={fieldStyle}>마진율 (%)<input type="number" value={marginRate} onChange={e => setMarginRate(Number(e.target.value))} /></label><label style={fieldStyle}>올림 단위<input type="number" value={roundUnit} onChange={e => setRoundUnit(Number(e.target.value))} /></label></div><button style={{ ...buttonStyle, marginTop: 12 }} onClick={() => setProducts(current => current.map(product => ({ ...product, finalPrice: marginRate === 0 ? product.basePrice : Math.ceil((product.basePrice * (1 + marginRate / 100)) / Math.max(1, roundUnit)) * Math.max(1, roundUnit) })))}>전체 가격 적용</button></div>}{step === "review" && <div className="full"><h2>개별 엑셀 다운로드</h2><p>카테고리별 최대 500개 단위입니다. ZIP 압축은 사용하지 않습니다.</p><div style={{ display: "grid", gap: 8 }}>{downloadPages.map(page => <button key={page.key} disabled={busy} style={{ ...buttonStyle, textAlign: "left" }} onClick={() => downloadPage(page)}>카테고리 {page.category} · {page.page}페이지 · {page.items.length}개 다운로드</button>)}</div></div>}</>}<div className="status full">{busy ? "처리 중… " : ""}{status}</div></section></main>;
}

function Input({ label, value: initial, apply }: { label: string; value: string | number; apply: (value: string) => void }) { const [value, setValue] = useState(String(initial ?? "")); return <label style={fieldStyle}>{label}<input value={value} onChange={e => setValue(e.target.value)} /><button type="button" style={buttonStyle} onClick={() => apply(value)}>이 묶음에 적용</button></label>; }
function Editor({ title, count, sample, children }: { title: string; count: number; sample: Product[]; children: React.ReactNode }) { return <div style={{ border: "1px solid #ddd", borderRadius: 12, padding: 16, contain: "layout paint" }}><h2>{title}</h2><p>{count}개 상품을 한 번에 수정합니다.</p><details><summary>상품명 확인</summary>{sample.map(product => <p key={product.id}>{product.productName}</p>)}</details><div style={{ marginTop: 16 }}>{children}</div></div>; }
